#include "main.h"

/**
 * rev_string - function that reverses a string
 *
 * @s: string variable to be reversed
 *
 * Return: no return
 */

void rev_string(char *s)
{
	char c;

	c = *(x + begin);
	*(x + begin) = *(x + end);
	*(x + end) = c;
	reverse(x, ++begin, --end);
}
