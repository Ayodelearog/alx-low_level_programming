#include "main.h"
/**
 * main - a program that prints all alphabets in lowercase
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char i;

	if (i = 'a'; i < 'z'; i++)
	{
		print_alphabet(i);
	}
	_putchar('\n');
	return (0);
}
